---
wildfly-configuration:
  extension:
    org.jboss.as.failure:
      module: org.jboss.as.failure
  subsystem:
    lists:
      list:
        my-list:
          strings:
            - foo
            - bar
